package pin.components;
/**
 * Interface representing a component of a password.
 * Provides a method for generating a string component.
 */
public interface PasswordComponent {
    /**
     * Generates a string component.
     *
     * @return A string component for password generation.
     */
    String generateComponent();
}